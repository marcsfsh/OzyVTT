# ADR-020: Combat rules engine — validated action resolution, effects, typed damage, and the dying state

## Status

Accepted — 2026-07-18. Amended the same day (second slice, same PR): reaction
prompts, incapacitation gating, the available-actions read, and archive v3 — see
"Amendment" below.

## Context and decision drivers

A forensic comparison of two archived runs of the same encounter (a manual GM run and a rules-aware recreation driven through the integration API) showed that the engine recorded mechanics but did not own them: the manual run performed two Frenzy attacks plus a Greataxe attack in one round-one turn with no Rage active, resolved Extra Attack as unrelated single rolls, applied crocodile damage to a raging barbarian with no resistance, never created the Bite grapple as state, and used seven manual heals (five of exactly 1 HP) as a stand-in for a dying state. None of this was blocked, warned about, or guided. The automated recreation proved the low-level API could emulate the rules — at a cost of 120 commands for a three-round fight, with the rules living in an external script instead of the server.

The owner's direction for this milestone: combat should "just work" — the legal action is the easiest path, unavailable actions are clearly explained, and the GM keeps an explicit, audited override for homebrew and intentional exceptions. This supersedes the earlier blanket posture that action economy is "tracked, never enforced" (ADR-008's reference level; BUILD_PLAN §13.4 "do not hard-block extra actions"), which these runs showed delegates too much rules bookkeeping to the GM's memory. Design precedent for the typed-mechanics vocabulary was validated against MIT-licensed sibling projects (notably cbgfx/beholden's typed-effects compendium schema) — patterns only, no code or data reuse.

## Considered options

- Keep reference-level tracking and improve display only: preserves the old posture but cannot prevent any of the observed error classes.
- A high-level "execute action" API that atomically rolls every attack, applies damage, and resolves prompts in one transaction (the gap-analysis report's original proposal): maximally guided, but reshapes the one-command-one-roll journal/archive contract, breaks replay of existing archives, and removes the GM's between-rolls target choice.
- Validated resolution over the existing command surface (chosen): `action.resolve` stays one roll burst per command, but validates against engine-owned state before rolling, with a per-encounter enforcement mode and an audited override.

## Decision

The server owns combat rules state and validates structured action resolution against it (extending ADR-008, whose representation rules are unchanged: declarative vocabulary, bounded dice grammar, no imported executable code).

1. **Structured mechanics vocabulary** (additive-optional on `ActorDefinition` schemaVersion 1, per ADR-007): `attack.count` (Extra Attack, beams), `attack.criticalBonusDice` (Savage Attacks), `multiattack` composition, `onHit` condition riders with escape DCs and size caps, `targetRules`, `grants` (self effects like Rage/Reckless with typed modifiers, durations, and on-end grants), `requiresEffectTag`, `uses` (per turn/encounter/long-rest with shared pools), and typed defense lists (`damageResistances`/`damageImmunities`/`damageVulnerabilities`/`conditionImmunities`). The SRD ETL emits these from confident statblock patterns only (126 of 178 Multiattacks, 47 on-hit riders, 146 monsters with typed defenses); everything else stays inert prose.
2. **Rules modes with an audited override.** Each encounter carries `rulesMode`: `strict` (default — invalid structured resolutions are rejected with machine-readable `blocked {rule, message, overridable}` details), `assisted` (allowed, conflicts logged as warnings), `freeform` (no checks). Any rejection can be bypassed by resending with `override: {reason}`, which is prominently recorded in the combat log (kind `override`) and the journal. An actor whose stat block has a prose-only Multiattack degrades action-slot violations to warnings even in strict mode — the engine never polices rules it cannot see. Manual commands (`turn.use`, amount-only damage, `actor.set-condition`, `actor.set-hp`) remain free escape hatches; un-marking the action slot also clears the open compound-action instance.
3. **Action instances.** Resolving an action-activation action with a free action slot consumes the slot once and opens an instance of remaining components: a generic `attack` pool for `attack.count` (so Extra Attack may legally mix weapons), actionId-keyed components for `multiattack` (Bite + Tail). Further resolves consume components; exhausted instances reject with "no attacks remaining". One command still equals one roll burst, preserving journal/archive semantics and sequential target choice.
4. **Persistent effects.** `Actor.effects` holds engine-owned `EffectInstance`s (name, tags, source links, duration, typed modifiers, linked conditions, escape DC, on-end grants, `endsWithTag` cascades). The engine ends them: durations expire at the source's turn start, a source at 0 HP releases what it sustains (grapples, its own Rage), `endsWithTag` cascades (Frenzy's marker ends with the Rage, firing its Exhaustion exactly once), encounter end sweeps the fight's combatants (on-end grants fire — a documented simplification), and grants from the same source actor + action refresh rather than stack. Sweeps are scoped to the current initiative so parked scenes keep their effects. On-hit riders auto-apply conditions at resolve time — the same owner-approved carve-out class as save auto-apply.
5. **Typed damage at apply time.** `actor.apply-damage` accepts optional typed `parts`; the server applies immunity → resistance-halving → vulnerability (resistance and vulnerability on one type cancel) from definition defenses plus active effects, then temp HP, and returns/logs the per-part breakdown ("17 bludgeoning → 8, resistance: Rage") — never a silent adjustment. The bare `amount` stays exact for manual corrections. Pending saves carry typed parts and halve per part on success.
6. **Advantage aggregation with explanations.** Attack resolution aggregates advantage/disadvantage sources (effect modifiers — `attack-advantage` is evaluated only on the bearer's own turn, Reckless semantics; incoming-attack-advantage on the target; prone with the 2024 distance rule via authoritative token distance; restrained/poisoned/blinded/stunned/paralyzed/petrified/unconscious; unconscious + adjacent upgrades hits to crits), cancels per 5e, rolls `2d20kh1/kl1`, and reports every contributing source. An explicit GM `rollMode` choice wins.
7. **The dying state.** Damage dropping a player character to 0 applies Unconscious + Prone and initializes death saves (instant death on overflow ≥ max HP); damage while dying adds failures (two on a crit; stability breaks and counters restart from zero per the SRD reset rule); `death-save.roll` runs the d20 pipeline (20 → 1 HP + conscious; 1 → two failures; three successes → stable with counters reset; three failures → dead); healing or set-hp above 0 clears the dying state (Prone stays). Non-player combatants at 0 are "defeated" (effects they sustain end; no death saves). All five HP entry points share this machine.
8. **Command surface.** New commands `effect.add`, `effect.end`, `death-save.roll`, `encounter.set-rules-mode`, `actor.rest` (long rest: effects end first, HP/uses/dying reset, Exhaustion −1) ride the shared operations layer, both transports, the OpenAPI contract, and the archive journal like every other command (ADR-016 no-fork). Timeline restores and the dirty comparator cover effects, death saves, and spent uses; player projections strip effect source ids, mask hidden source names, and expose limited-use spending only for the claimed character.

## Consequences and tradeoffs

The published `turn.use` semantics changed from "(tracked, never enforced)" to "manual toggle; structured resolution validates per rules mode" — integrations replaying old journals (or scripting freely) should set `freeform` mode first, and a mid-flight encounter upgraded under this release parses to `strict` and may see first rejections until the GM overrides or switches modes. Enforcement applies only on the acting combatant's own turn; off-turn resolutions (opportunity attacks, GM improvisation) stay ungated. Prose-only mechanics remain unenforced by design.

Explicitly unsupported after this milestone (documented, not accidental — superseded in part by the 2026-07-19 amendment below): reaction triggers beyond hit-by-attack/half-damage (opportunity attacks, Parry-style AC responses, damage-taken triggers), triggered features (Relentless Endurance, Dark One's Blessing, Halfling Lucky, Sneak Attack *eligibility* — only its once-per-turn pool ships), concentration and target-scoped effects (Hex), movement/range/reach/line-of-sight legality, rolled grapple escapes (the escape DC displays; ending the effect is manual), incapacitated-ends-grapple (only 0 HP releases), player-invoked `action.resolve`, per-encounter monster death policies, ruleset (2014/2024) declarations and import compatibility levels, structured recharge/legendary actions, and beam-count-by-level expressions (static counts in definitions). Non-turn-scoped always-on attack advantage (e.g. Invisibility) needs a new modifier flag — do not reuse `attack-advantage` for it. The remaining-work roadmap lives in `docs/product/rules-engine-followup-assessment.md`.

## Amendment — 2026-07-18 (second slice, same PR)

9. **Reaction prompts.** Declared reactions (`reaction: {trigger: "hit-by-attack", response: "half-damage"}` — Uncanny Dodge) become first-class pending prompts on the pendingSaves template: a qualifying hit (structured attack, damage > 0, reactor's reaction free, reactor not incapacitated, mode not freeform) parks its rolled typed damage on `combat.pendingReactions` *instead of* the runner's apply button. `reaction.answer` (GM anyone; a player their claimed character) applies the damage either halved per part (use — spends the reaction) or in full (decline); the halving happens before defenses, so resistance still applies on top. `reaction.dismiss` (GM) drops a prompt without applying — the escape hatch for damage entered by hand. Prompts persist across turn changes until answered so parked damage is never silently lost; player projections carry only the player's own prompts, source ids stripped and hidden sources masked.
10. **Incapacitation gates the economy.** An actor with Incapacitated (or a condition that includes it: Paralyzed, Petrified, Stunned, Unconscious) violates `condition.incapacitated` for any action, bonus action, or reaction — handled by the same strict/assisted/override ladder as every rule.
11. **Available-actions read.** `GET /game/actors/{actorId}/available-actions` (and the `actor:available-actions` socket read) reports per-action availability computed by the *shared* `evaluateActionEconomy` — the exact evaluation strict resolution enforces, so the report can never drift from enforcement. Rows carry machine-readable violations, uses remaining, and open-instance counts. GM-grade any combatant; a player only their claimed character (availability names stat-block internals). Target-specific rules need a target and are not pre-checked.
12. **Archive v3.** Encounter archives additively gain `postEncounterState` — the state after `encounter.end` ran (combat cleared, end-of-fight effect sweeps and their on-end grants landed). `finalState` (the last live picture) is unchanged; the fight's true aftermath was previously unrecorded.

## Amendment — 2026-07-19 (SRD gap closure, tiers A–D, same PR)

A full cross-audit of the SRD 5.2.1 combat chapter, rules-glossary actions, and conditions appendix against the engine closed the verified gaps in four tiers. Everything below rides the existing machinery — the rules ladder (strict/assisted/freeform with audited overrides), explainable advantage/disadvantage sources, the additive-with-defaults state rules, and the projection allowlists. Where a rule needs a subsystem the app deliberately lacks (vision/light, terrain, hands/inventory), the GM supplies the judgment call as a command input and the server applies the math.

13. **Condition completeness (Tier A).** Every SRD condition now has its mechanical teeth: frightened → attack disadvantage (applied whenever active — line-of-sight is GM adjudication, a documented simplification); invisible → own-attack advantage / incoming disadvantage; grappled attacker → disadvantage against everyone but the grappler (source-linked effect names the grappler; a hand-set condition falls back to "grappler unknown" disadvantage against all); paralyzed joins unconscious in the within-5-ft auto-crit; paralyzed/petrified/stunned/unconscious auto-fail Strength and Dexterity saves (the manual-total path deliberately bypasses — the GM escape hatch); restrained → Dex-save disadvantage; exhaustion applies −2 × level to attacks and saves, −5 ft × level to Speed, and level 6 is death; petrified adds resist-all + poison immunity at damage time; `conditionImmunities` (GM-only knowledge, stripped from player projections) skip-with-narration; charmed blocks targeting the linked charmer (`condition.charmed-charmer`).
14. **Builtin generic actions (Tier B).** The eleven 2024 generic actions plus Unarmed Strike variants ship as a code-side catalog (`builtin-actions.ts`, frozen ids — a replay contract) available to every combatant, definition or none; a stat block action with the same id shadows the builtin. Dodge/Disengage/Dash/Help/Ready grant tagged effects with typed modifiers (`voidWhileIncapacitated` — Dodge lapses per the SRD); Influence/Search/Study consume the action and roll the ability check; Magic/Utilize consume and narrate. Hide rolls Dex (Stealth) vs DC 15 → an Invisible-linked Hiding effect that any attack reveals (eligibility — cover, obscurement, sight lines — is GM adjudication). Help targets one ally with attack advantage until the helper's next turn (the SRD's next-attack-only scope widens to the round; documented). Unarmed Strike materializes Str + PB / flat 1 + Str; Unarmed Grapple/Shove force the target's better Str/Dex save vs 8 + Str + PB with a one-size cap, grapples landing as escapable source-linked effects (escape DC = the same formula); Escape Grapple rolls best of Athletics/Acrobatics vs the DC; an incapacitated grappler releases its holds. Ready parks a free-text trigger; the reactor's next off-turn resolve spends the reaction and releases it.
15. **Movement and opportunity attacks (Tier C).** `Actor.speedFeet` (optional — unknown speed skips checks, the unmeasurable pattern) seeds from definitions with a GM `actor.set-speed` command; `turn.movementUsedFeet` accumulates on-turn willing moves against `max(0, speed − 5 × exhaustion)`, ×2 while Dashing, 0 while grappled/restrained/unconscious/paralyzed/petrified/stunned; overruns are `movement.exceeds-speed` violations with a GM-only override, and standing from Prone charges half Speed (`movement.stand-up-cost`). Leaving an able enemy's melee reach without Disengaging opens a `leaves-reach` reaction prompt (side heuristic: player-characters vs monsters, npcs excluded; the move completes first — arrival timing is a documented approximation); answering resolves a real melee attack (declared action, first melee, or the builtin Unarmed Strike) against the mover off-turn and auto-applies its damage — which the mover's own Uncanny Dodge can intercept. A hidden mover prompts no one (viewer safety); the GM resolves that retreat manually. Range/reach validate at resolve when measurable: melee beyond reach (`range.out-of-reach`), ranged beyond maximum (`range.out-of-range`), long range and ranged-in-close-combat disadvantage sources; the ETL now splits "80/320" into normal/maximum bands.
16. **Cover, concentration, surprise, rests (Tier D).** Cover is GM-selected per resolve (no line-of-sight engine): half +2 / three-quarters +5 to AC and Dex saves, total an overridable `cover.total` block. Concentration: `concentration`-flagged effects are one-at-a-time per source, damage prompts a CON save at DC `min(30, max(10, ⌊damage/2⌋))` whose committed failure ends the sustained effects, and incapacitation or 0 HP breaks concentration immediately. Surprise (2024): a surprised initiative entry rolls `min(d20, d20)`. Short rests re-arm `per: "short-rest"` pools only — hit dice are unmodeled, so no HP change (the GM heals manually).
17. **Environment and mercy rules (Tier D niche).** A GM `encounter.set-environment` toggle marks the fight underwater: melee disadvantage unless the weapon deals piercing damage (the SRD's weapon list generalized to its shared damage type), ranged auto-miss beyond normal range (`range.underwater`), fire resistance for every combatant. `actor.apply-damage` accepts `nonlethal`: a drop to 0 knocks out — Unconscious and stable, no death saves, no defeat. The dice panel gains a falling-damage helper (`min(⌊feet/10⌋, 20)d6` rolled server-side, Prone reminder).

Still explicitly unsupported after this amendment (deliberate, roadmapped in the follow-up assessment): two-weapon fighting and the Light property (no hands/inventory model), weapon mastery, mounted combat, jumping, burning/suffocation timers, breaking objects, readying a spell (concentration while held), hit dice, vision/light/line-of-sight and auto-derived cover, and difficult terrain (needs terrain data). Underwater speed halving is folded into the GM's speed adjustments rather than automated.

## Amendment — 2026-07-19 (Foundry-dnd5e adoption pack: recharge, legendary resources, hit dice; same PR)

A design study of Foundry's dnd5e system (MIT code; studied, never copied) identified three monster/rest mechanics the engine lacked. All three ride the existing machinery — `actionUses` pools, the turn-start hook, the save answer flow, the additive-state and new-command checklists.

18. **Recharge abilities.** `ActionSchema.uses.per` gains `"recharge"` with a 2–6 d6 threshold (a superRefine pins the pairing). The ETL emits structured pools instead of prose-only names: `RECHARGE_ON_ROLL` → `{limit 1, per recharge}`; `RECHARGE` **with a numeric param** is also a die-range recharge (an upstream data quirk — basilisk/medusa Petrifying Gaze, blink dog Teleport and ten others print "Recharge X–6" in the SRD; the param carries the threshold), leaving one true rest-recharge (cloaker Phantasms) mapped to `short-rest`; `N/Day` maps to `long-rest` (the app has no calendar — a day is a long rest, a documented simplification shared with Legendary Resistance). 86 recharge and 28 rest-scoped pools across the 330-monster bundle; double-printed "(Recharge …)" name suffixes are fixed. At the start of the owner's turn `nextInitiativeTurn` rolls the d6 per spent pool (deps threaded from `initiative.next` and `turn.end` with the audited RNG — the deterministic test queue drives it) and narrates the die either way; `previousInitiativeTurn` deliberately never rolls (GM correction; the Time Machine is the undo). Encounter start and both rests re-arm pools.
19. **Legendary actions and Legendary Resistance.** `ActionSchema` gains `legendary {cost}` (all 82 SRD rows cost 1) and definitions/actors gain `legendary {actionsPerRound, resistancesPerDay}` (every SRD legendary creature prints 3 per round — the "(4 in Lair)" bump stays prose; 32 creatures carry resistance at the printed non-lair N). The per-round pool is `combat.legendaryUsed` (actorId → spent), refreshed exactly like `reactionsUsed` — the incoming actor's entry clears at its own turn start, in both turn directions — carried through scene park/resume, restored by timeline rewinds, and **stripped from player projections** (monster resources are GM knowledge, the `actionUses` rationale). `evaluateActionEconomy` blocks own-turn use (`legendary.own-turn` — legendary actions happen on other creatures' turns) and over-pool spends (`legendary.no-actions-remaining`) through the rules ladder; a GM-only `turn.use-legendary` sets the pool by hand (the spend path for prose-only legendary actions). Legendary Resistance rides `actionUses["legendary-resistance"]`: `save.answer` accepts a GM-only commit flag that forces the success outcome after a previewed failure — the success consequences (half damage where the save halves) still apply, a natural success spends nothing, and a long rest re-arms the daily pool. The tracker offers off-turn legendary creatures as one-tap console switches (⭐ strip) and failed-save previews grow a "Legendary Resistance (N left)" commit.
20. **Hit Point Dice.** `Actor.hitDice {die, maximum, remaining}` (nullable — absent means unmodeled and rests behave exactly as before) seeds from the definition's hit-point formula at roster time. The new `actor.spend-hit-dice` command (GM any actor; players their claimed character) rolls the faces **before** the store executes (the `dice.roll` idempotency pattern), heals `max(1, face + Con modifier)` per die (SRD minimum) through `healActor`'s single-sourced rules, decrements the pool, and lands the dice in the shared roll history (hidden actors GM-only); it is rejected mid-encounter, matching the rest guard. A long rest restores all spent dice (SRD 5.2.1 "Regain All HP"); short rests still change no HP by themselves — spending is the explicit act. The pool reaches only the owning player and is excluded from the timeline's restorable slice (an out-of-combat resource). The roster grows the previously missing rest UI: GM Short/Long rest buttons and a hit-dice stepper per card, mirrored on the player's own card.

## Mobile, security, and visibility impact

All validation, rolls, and effect lifecycle run server-side; phones and desktops see identical outcomes. Blocked rows stay tappable in the GM UI — the server rejection opens the one-tap override dialog, so the client never gates (server authority, CLAUDE.md rule 2). New projection fields are explicit allowlists: effects reach players without source actor/action ids (hidden sources masked as "A hidden threat"), a hidden combatant's turn instance and per-turn uses project as idle, limited-use spending travels only to the owning player, and the public viewer projection is unchanged.

## Migration / reversibility

All state additions are additive-with-defaults on schemaVersion 1: existing campaigns, turn snapshots, and archives parse unchanged, and old journals replay under `freeform` mode. The bundle regeneration is deterministic and reviewed in git. Reverting enforcement is a one-line default change (`rulesMode` to `freeform`) with no state migration; removing the mechanics fields would require a content-schema review per ADR-007.

## Validation evidence

`packages/rules-5e/test/combat.test.ts` (damage adjustment, aggregation, death-save transitions), `apps/server/test/combat-rules-regression.test.ts` (35 tests derived from the two replay archives — the amendment added Uncanny Dodge prompt/answer/decline/dismiss/eligibility/persistence, incapacitation gating, and availability-report coverage to the original 25: the round-one double-Frenzy block and legal path, Extra Attack instances with weapon mixing, Reckless advantage both directions with expiry, Rage +2 and 17→8 resistance breakdown, crit + Savage Attacks dice, Sneak Attack once per turn, crocodile Multiattack with source-linked grapple + Tail exclusion + defeat cleanup, the dying state and healing, audited override, Frenzy's single Exhaustion via the endsWithTag cascade, parked-scene effect safety, timeline restore of rules state, prose-Multiattack degradation, Eldritch Blast beams), plus updated contract/scope/projection pins and bundle golden checks (`giant-crocodile` mechanics, `adult-red-dragon` typed immunities, coverage floors). Full suites: `npm run check`, `npm test`, `npm run build` green.

The adoption-pack amendment grew the suite to 104 tests: recharge (spend → blocked → low-roll stays spent → threshold re-arms with narrated dice, armed pools never roll, encounter start and both rests re-arm, the schema pins per↔threshold pairing) with bundle golden checks (wyrmling Cold Breath `{limit 1, per recharge, recharge 5}`, the basilisk/medusa upstream-quirk reclassification, no double-printed suffixes, ≥80-pool coverage floor); legendary (off-turn spends and costs, own-turn and over-pool blocks, refresh in both turn directions, GM-only manual set, timeline dirtying + rewind restore, player projection leak checks, Legendary Resistance preview→commit flip with half-damage-on-success evidence, exhausted-pool rejection, natural-success no-spend, long-rest re-arm, 30/82/32 bundle counts); hit dice (formula seeding incl. the giant crocodile, per-die minimum-1 clamping at negative Con, pool/encounter/unmodeled rejections, long-rest refill, owner-only projection with legacy-save default fill). Live Playwright smokes verified the legendary console switch and the roster rest flow (spend 2 → heal + decrement + roll record → long-rest refill) at desktop and 375 px.

The 2026-07-19 amendment grew the regression suite to 81 tests, each new block citing its SRD section: Tier A condition modifiers (frightened/invisible/grappled-vs-grappler labels, paralyzed auto-crit, auto-fail and restrained-disadvantage saves with `2d20kl1` evidence, exhaustion penalties and level-6 death, petrified resist-all breakdown, immunity skip narration, charmed-charmer block), Tier B builtins (Dodge lapse while stunned, Help expiry at the helper's turn, Hide → Invisible → reveal, unarmed grapple → failed save → escape → stunned-grappler release, size cap, Ready release off-turn, definition-less combatants), Tier C movement (budget accumulation, Dash ×2, Speed-0 grapple, exhaustion subtraction, off-turn freedom, prone stand cost, OA prompts with Disengage/spent-reaction/incapacitated/hidden-mover suppression, OA answered by a real attack that Uncanny Dodge can intercept, range/reach/long-range/close-combat checks), Tier D (cover ±AC and Dex-save bonus with total-cover override, concentration one-at-a-time/DC math/fail-ends/stun-breaks/0-HP-breaks, surprise `min(d20, d20)`, short-rest pool re-arm scoping, underwater melee/ranged/fire rules, knock-out for characters and monsters), plus contract pins for the new command (`encounter.set-environment`), the projection pin for the player-visible `underwater` flag, and legacy-state default fills.
