# The server listens on every local interface so phones and laptops on the LAN can join.
PORT=3001
# Set this only when developing the browser client separately.
CLIENT_ORIGIN=http://localhost:5173
# Keep this directory outside source control. It contains the password hash and game state.
DATA_DIR=./data
