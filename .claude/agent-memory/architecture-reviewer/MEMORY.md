# architecture-reviewer — memory

Curated index of durable architectural learnings for this repo. Keep this file short; put detail
in sibling topic files and link them here. Update after substantive reviews.

## Invariants confirmed / cross-refs

- Server authority (ADR-0001), contract-lives-once in `packages/domain` (ADR-0005), projection is
  the GM/player/viewer boundary (ADR-0005/0011), public API reuses the UI path never a fork
  (ADR-0016), transactional persistence (ADR-0006), device parity (ADR-0014).

## Recurring risks / anti-patterns

_(none recorded yet)_

## Notes

_(add per-review findings)_
